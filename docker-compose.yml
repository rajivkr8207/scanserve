version: "3.9"

services:

  frontend:
    build:
      context: ./frontend
    container_name: scanserve-frontend
    ports:
      - "3000:3000"

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: scanserve-backend
    ports:
      - "8000:8000"